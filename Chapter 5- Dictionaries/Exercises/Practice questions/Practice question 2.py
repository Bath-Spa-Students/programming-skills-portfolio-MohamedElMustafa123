personal_info = {'Name': 'Mohamed',
                 'Age': 19,
                 'Hobby': 'Skating'
                 }
#using the function .keys() we can extract the keys from the dictionary
for key in personal_info.keys():
    print(key)